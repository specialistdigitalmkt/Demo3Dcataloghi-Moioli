Input — a labeled text field for forms (quote requests, contact).

```jsx
<Input label="Email" type="email" placeholder="nome@azienda.it" />
<Input label="Telefono" error="Campo obbligatorio" />
```

3px radius, 1px border stepping to navy on focus and red on error. Label is an uppercase eyebrow-style caption above the field.

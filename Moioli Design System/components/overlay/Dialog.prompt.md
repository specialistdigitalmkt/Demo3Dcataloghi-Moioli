Dialog — a centered modal for confirmations, positioned relative to a `position: relative` container.

```jsx
<Dialog open={open} title="Richiesta inviata" onClose={() => setOpen(false)}>Ti risponderemo entro 2 giorni lavorativi.</Dialog>
```
